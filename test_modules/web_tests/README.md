Contains definitions for YTS web tests.

YTS web tests run fully inside Cobalt, on a standalone empty HTML page (not the
YouTube app itself). They contain no logic outside the device, and no direct
dependencies on actual YouTube app behavior.
